import m from "mithril";

import Footer from "components/homefooter";

var Messages = {
  view: vnode => (
    <section>
      Messages feeds goes here...
      <Footer />
    </section>
  )
};

export default Messages;
