import m from "mithril";
import Shell from "containers/shell";
import Footer from "components/homefooter";

var CreatePost = {
  view: vnode => (
    <section>
      Videos feeds goes here...
      <Footer />
    </section>
  )
};

export default CreatePost;
